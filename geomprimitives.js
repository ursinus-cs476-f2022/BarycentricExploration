//Purpose: The engine behind the 3D primitive operations for Mini Assignment 1

vec3 = glMatrix.vec3;

/**
 * Figure out of two circles intersect
 * 
 * @param {vec3} c1 Center of first circle
 * @param {float} r1 Radius of first circle
 * @param {vec3} c2 Center of second circle
 * @param {float} r2 Radius of second circle
 */
function circlesIntersect(c1, r1, c2, r2) {
	return false; // TODO: Fill this in
}